apiVersion: 1

datasources:
  - name: Databend
    type: databendlabs-databend-datasource
    access: proxy
    isDefault: true
    jsonData:
      dsn: "databend://databend:databend@databend:8000/default?sslmode=disable"
    editable: true
